name: evosim experiment

# Runs headless.js on GitHub-hosted runners instead of the Claude Code
# session's own sandbox. Point of this: each seed gets its own dedicated
# runner (real parallelism, not N processes sharing 4 cores), it runs
# whether or not any Claude session is open, and it costs zero tokens and
# zero sandbox CPU. Trigger by hand (Actions tab -> evosim experiment ->
# Run workflow) or via the API/MCP (actions_run_trigger, method
# run_workflow) once this file is on the default branch — GitHub only
# recognizes workflow_dispatch inputs for workflows present there.
#
# Does not touch evosim-v0_47_0.html and does not ship anything by itself:
# it writes a JSON log per seed as a downloadable artifact, same shape
# exportLog() writes on the phone. Someone (owner or Claude) still reads the
# digest and decides what happens next.

on:
  workflow_dispatch:
    inputs:
      build:
        description: 'Build file to run'
        default: 'evosim-v0_47_0.html'
        required: true
      seeds:
        description: 'Comma-separated seeds, e.g. 4001,4002,4003'
        required: true
      days:
        description: 'Target sim-days per seed'
        default: '1000'
        required: true
      cfg:
        description: 'Path to a CFG patch JSON in the repo (optional)'
        required: false
      label:
        description: 'Run label, used in artifact names'
        default: 'run'
        required: true
      progress_days:
        description: 'How often (sim-days) to write progress'
        default: '20'
        required: false

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      seed_matrix: ${{ steps.parse.outputs.matrix }}
    steps:
      - id: parse
        run: |
          seeds='${{ inputs.seeds }}'
          json=$(python3 -c "import json,sys; print(json.dumps([s.strip() for s in '$seeds'.split(',') if s.strip()]))")
          echo "matrix=$json" >> "$GITHUB_OUTPUT"

  run:
    needs: setup
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        seed: ${{ fromJson(needs.setup.outputs.seed_matrix) }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: run headless.js
        run: |
          mkdir -p out
          CFG_ARGS=""
          if [ -n "${{ inputs.cfg }}" ]; then CFG_ARGS="--cfg ${{ inputs.cfg }}"; fi
          node headless.js \
            --build "${{ inputs.build }}" \
            --seed "${{ matrix.seed }}" \
            --days "${{ inputs.days }}" \
            --progress-days "${{ inputs.progress_days }}" \
            $CFG_ARGS \
            --out "out/seed-${{ matrix.seed }}.json"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.label }}-seed-${{ matrix.seed }}
          path: out/
          retention-days: 30

  digest:
    needs: run
    runs-on: ubuntu-latest
    if: always()
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: ${{ inputs.label }}-seed-*
          path: logs
          merge-multiple: true
      - name: analyze.py cross-seed digest
        run: |
          ls -la logs/
          python3 analyze.py logs/seed-*.json | tee digest.txt
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.label }}-digest
          path: digest.txt
          retention-days: 30
